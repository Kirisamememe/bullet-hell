{
  "name": "danma-rengo",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "build": "bun build src/main.ts --outdir=dist --target=browser --minify && cp public/index.html dist/index.html",
    "start": "bun run dist/main.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
